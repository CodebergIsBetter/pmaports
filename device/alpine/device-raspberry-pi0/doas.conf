# postmarketos-base normally arranges for the install user to be able to become
# root. This device installs no postmarketOS packages (deviceinfo_alpine_only),
# and pmbootstrap leaves the root account locked, so without this a headless
# user could log in over SSH and then do nothing useful.
permit persist :wheel
