<?xml version="1.0" encoding="UTF-8"?>
<!--
  xfdesktop's built-in default backdrop is
  /usr/share/backgrounds/xfce/xfce-x.svg, and nothing in this image can decode
  SVG: gdk-pixbuf has only PNG, JPEG and GIF built in, and librsvg dropped its
  gdk-pixbuf loader upstream, so Alpine's librsvg ships the library alone. The
  image fails to load and xfdesktop paints its solid fallback colour, which
  looks like a broken session rather than a missing wallpaper.

  xfce-blue.jpg is the only raster file the backgrounds package ships, so point
  at that. HDMI-A-1 is the name of this board's only connector under vc4 KMS;
  monitor0 is there for anything that reports differently.
-->
<channel name="xfce4-desktop" version="1.0">
  <property name="backdrop" type="empty">
    <property name="single-workspace-mode" type="bool" value="true"/>
    <property name="single-workspace-number" type="int" value="0"/>
    <property name="screen0" type="empty">
      <property name="monitorHDMI-A-1" type="empty">
        <property name="workspace0" type="empty">
          <property name="last-image" type="string" value="/usr/share/backgrounds/xfce/xfce-blue.jpg"/>
          <property name="image-style" type="int" value="5"/>
        </property>
      </property>
      <property name="monitor0" type="empty">
        <property name="workspace0" type="empty">
          <property name="last-image" type="string" value="/usr/share/backgrounds/xfce/xfce-blue.jpg"/>
          <property name="image-style" type="int" value="5"/>
        </property>
      </property>
    </property>
  </property>
</channel>
